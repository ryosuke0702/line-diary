<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    / <span class= "btn btn-success"><%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %></sapn>
  <% end %>
</li>